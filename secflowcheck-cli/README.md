# SecFlowCheck CLI

CLI tool for analyzing CI/CD pipelines using SecFlowCheck.

## Usage

```bash
secflowcheck pipeline.yml
secflowcheck pipeline.yml --fail-on-critical
secflowcheck pipeline.yml --format json
